
[build-system]
requires = ["setuptools>=42", "wheel", "torch", "transformers", "datasets", "accelerate"]
build-backend = "setuptools.build_meta"

[project]
name = "seq2seqdistill"
version = "0.1.7"
readme = "README.md"
